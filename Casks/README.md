# homebrew-ytdownloader

Homebrew tap for [YTDownloader](https://github.com/mrKangHo/youtubeDownloader), a native macOS GUI for `yt-dlp`.

## Install

```sh
brew tap mrKangHo/ytdownloader
brew install --cask ytdownloader
```

This also installs `yt-dlp` and `ffmpeg` as dependencies.
